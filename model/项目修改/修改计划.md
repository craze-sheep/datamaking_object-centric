- config.py — 改默认值：history_length: int = 12  →  history_length: int = 24
- attr_dim需要修改成新维度：ground, sphere, cube, cylinder, ramp, wall
- 接下来应该改 model/ai_model/dataset.py 的类型编码和 force_matrix 对齐逻辑。
- mess问题
```json
数据集和脚本都已修改
static=true:
  ground: mass = null      31880 个
  ramp:   mass = number     2640 个
  wall:   mass = number     5750 个
```
- object_id和真实物体对应