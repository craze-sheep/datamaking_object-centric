# 物理视频世界模型：研究方向与技术路线

## 研究方向

主要聚焦视频世界模型的物理常识、object-centric 表征学习与因果推理问题，围绕可控物理交互场景构建、物理条件视频预测模型和下游评估体系开展研究，探究视觉预测模型对物体运动规律、碰撞事件和因果关系的建模能力。我个人全流程负责基于 PyBullet 物理引擎与 Kubric 渲染框架搭建参数化的物理视频数据生成管线，遵循物理直觉递进范式设计了自由落体、滑动摩擦、斜面运动、墙面反弹、多体碰撞、连锁碰撞（牛顿摆）及遮挡/非碰撞反事实等 8 类场景、60+ 子任务，共计 31,880 条样本，并保留物体属性、实例分割 mask、深度图、逐帧运动状态（6DoF 位姿、速度、合力）及物体间碰撞力矩阵等多模态标注；同时设计面向该数据集的 PyTorch 数据加载、训练和评估流程，不预设单一模型架构，可支持 Transformer、SSM、U-Net、diffusion 或 slot-based 模型作为预测基线与迭代方案，最终形成了 **物理视频数据生成 → 多模态数据加载 → baseline 训练 → 视频质量/物理一致性/碰撞检测评估** 的完整实验闭环。

---

## 一、数据集概况

- 总样本: 31,880 个
- 8 个场景 (S1-S8), 每场景 7-14 个 level
- 分辨率: 128x128, 12fps, 36帧/clip
- 物体类型: ground, cube, sphere (最多7个物体/场景)

**7个数据模态:**

| 模态 | 详情 |
|------|------|
| RGB 视频 | .mp4, 128x128, 36帧 |
| 深度图 | .npz, 36x128x128, float32 |
| 分割 mask | 每物体一个 .npz, 128x128, uint8 |
| 物体静态属性 | type, size, radius, mass, friction |
| 物体动态状态 | position(3D), quaternion(4D), velocity(3D), angular_velocity(3D), resultant_force(3D), visible_area |
| 力矩阵 | 物体间3D力向量, 10x10矩阵 |
| 物理元数据 | gravity, physics_hz(240), camera |


## 二、问题诊断：为什么 SAVi+GNS+Transformer 不够好

之前的方案: SAVi(视觉slot) + GNS(物理GNN) + Transformer(时序)

**3个致命问题:**
1. SAVi 只吃 RGB → 完全忽略 depth, mask, physics state, force matrix
2. GNS 是粒子模型 → 不理解刚体 (sphere/cube), 不用物体属性
3. 三段拼接 → 无法端到端训练, 信息在接口处丢失
4. SAVi 是 JAX → 项目是 PyTorch, 改造成本高

**数据利用率:**

| 模态 | SAVi | GNS | 你的数据 |
|------|------|-----|----------|
| RGB | ✅ 用 | ❌ | ✅ |
| 深度 | ❌ 浪费 | ❌ | ✅ |
| 分割mask | ⚠️ 学习 | ❌ | ✅ 有GT |
| 物体属性 | ❌ | ⚠️ 部分 | ✅ |
| 物理状态 | ❌ | ✅ 用 | ✅ |
| 力矩阵 | ❌ | ⚠️ 部分 | ✅ |
| 多视角 | ❌ | ❌ | ⚠️ 单视角 |

结论: SAVi+GNS 只能利用 ~30% 的数据。大量 GT 信息被浪费。


## 三、最相关框架/论文（按相关度排序）

### ★★★★★ 最相关 (2025-2026 新工作)

**1. SlotPi (2025.06) - Physics-informed Object-centric Reasoning**
- arxiv: 2506.10778
- 亮点: 把物理知识(Hamiltonian)注入 slot attention
- 问题: 代码未开源, 但架构思路可以直接借鉴
- 与数据的匹配度: ★★★★★ (slot + physics)

**2. Object-centric Diffusion for Physical Reasoning (2025.07)**
- arxiv: 2507.04920
- 亮点: 用扩散模型做物体级物理推理
- 问题: 可能没有depth/force矩阵的处理

**3. Learning Physical Dynamics for Object-centric Prediction (2024.03)**
- arxiv: 2403.10079
- 亮点: 无监督物体级物理动态预测
- 问题: 只用RGB, 没用depth/force

**4. Phantom: Physics-Infused Video Generation (2026.04)**
- arxiv: 2604.08503
- 亮点: 把物理规律注入视频生成的联合建模
- 问题: 偏生成, 不是预测

### ★★★★☆ 强相关 (物理+视觉)

**5. PhysTwin (ICCV 2025) ★409**
- github: Jianghanxiao/PhysTwin
- 亮点: 从视频做物理感知重建和模拟
- 问题: 偏可变形体, 数据是刚体

**6. PhysGen (ECCV 2024) ★349**
- github: stevenlsw/physgen
- 亮点: 用 Bullet 物理引擎做物理接地视频生成
- 思路: 可以反过来用——数据有GT物理状态, 可以训练一个 "逆PhysGen": 从视觉→物理参数

**7. Grounding GNS with Physical Sensor Observations (2023)**
- arxiv: 2302.11864
- 亮点: 把GNS和真实传感器数据结合
- 与数据的相关: dynamic states 就是 "传感器数据"

**8. Learning Rigid Dynamics with Face Interaction GNN (2022)**
- arxiv: 2212.03574
- 亮点: 专门为刚体碰撞设计的 GNN
- 与数据的相关: S5-S8 就是碰撞场景

### ★★★☆☆ 基础组件

| 组件 | 代码库 | 用途 |
|------|--------|------|
| 视觉特征提取 | DINOv2 (facebookresearch/dinov2) ★10k+ | RGB视觉特征 |
| 分割 | SAM2 (facebookresearch/sam2) ★13k+ | 已有GT mask, 可做扩展 |
| 深度 | Depth Anything V2 ★5k+ | 已有GT depth, 可做预训练 |
| GNN | PyTorch Geometric ★20k+ | 处理力矩阵和物体交互 |
| 刚体碰撞GNN | MarkoMile/gnn-physics-simulator ★16 | GNS PyTorch 实现 |


## 四、核心建议：不绑定 SlotFormer，先跑通 physics_pred baseline

项目中已有 SlotFormer 代码和预训练权重，但当前目标不是把研究路线绑定到 SlotFormer，而是先建立一个能稳定读取自建数据、训练预测模型、输出统一指标的 PyTorch baseline。SlotFormer 可以作为后续候选架构之一，但第一版应允许 Codex/实验人员在 Transformer、SSM、U-Net、diffusion、slot-based model 等方案中选择最合适的实现。

**第一版交付目标: `physics_pred/` 实验闭环**

```
physics_pred/
├── dataset.py          # 读取 database/{scene}/{level}/{sample_id}/ 下的所有模态
├── model.py            # 可替换的视频预测模型，支持 physics conditioning ablation
├── train.py            # 训练、验证、checkpoint、日志
├── eval.py             # 视频质量、物理一致性、碰撞检测评估
├── config.py           # 数据路径、shape、训练超参、loss 权重
└── utils.py            # padding、指标、可视化、碰撞标签等工具函数
```

**推荐 baseline: Physics-Conditioned Object Video Predictor**

```
输入层:
┌──────────────┐ ┌──────────────┐ ┌───────────────┐ ┌─────────────────┐
│RGB frames    │ │Depth maps    │ │Object masks   │ │Physics fields   │
│[T,3,H,W]     │ │[T,1,H,W]     │ │[T,N,H,W]      │ │static/dynamic/F │
└──────┬───────┘ └──────┬───────┘ └───────┬───────┘ └────────┬────────┘
       │                │                 │                  │
       └────────────────┴────────┬────────┴──────────────────┘
                                  │
                         ┌────────▼────────┐
                         │ Object encoder  │
                         │ CNN/ViT + ROI   │
                         └────────┬────────┘
                                  │
                         ┌────────▼────────┐
                         │ Dynamics model  │
                         │ Transformer/SSM │
                         │ or GNN hybrid   │
                         └────────┬────────┘
                                  │
                 ┌────────────────┼────────────────┐
          ┌──────▼──────┐ ┌───────▼───────┐ ┌──────▼──────┐
          │RGB forecast │ │Depth forecast │ │State/collision│
          │[Tp,3,H,W]   │ │[Tp,1,H,W]     │ │prediction    │
          └─────────────┘ └───────────────┘ └─────────────┘
```


## 五、具体实施路线

### Phase 1: 数据加载器和 baseline
- 在 `physics_pred/dataset.py` 中按真实目录结构读取样本。
- 明确每个字段 shape:
  - RGB: `[T, 3, H, W]`
  - depth: `[T, 1, H, W]`
  - masks: `[T, max_objects, H, W]`
  - static attributes: `[max_objects, attr_dim]`
  - dynamic states: `[T, max_objects, state_dim]`
  - force matrix: `[T, max_objects, max_objects, 3]`
- 处理变长物体数：padding 到 `max_objects=7`，同时输出有效物体 mask。
- 先训练最小 baseline：history 12 帧 → predict 12 帧。

### Phase 2: 物理条件输入
- 将质量、摩擦、恢复系数、尺寸/半径、物体类型、位置、姿态、速度、角速度、合力作为条件输入。
- 支持 with/without physics conditioning ablation，证明物理标注对视频预测和事件推理的贡献。
- 辅助预测未来位置/速度，避免模型只优化像素误差。

### Phase 3: 统一评估
- 视频质量：MSE、PSNR、SSIM。
- 物理一致性：预测位置/速度与 GT 的 MSE。
- 碰撞检测：从 force matrix 构造碰撞标签，计算 precision/recall/F1。
- 按场景 S1-S8 分组汇总，观察模型在哪些物理规律上失效。

### Phase 4: 迭代模型架构
- 如果 baseline 能跑通，再比较不同 dynamics module：
  - Transformer/Temporal ViT：稳健、实现简单。
  - SSM/Mamba-like：长序列效率高。
  - U-Net video predictor：像素预测基线强。
  - Diffusion video predictor：生成质量高，但训练成本更大。
  - SlotFormer/slot-based model：适合 object-centric 表征与物体级推理。
  - GNN hybrid：适合显式利用 force matrix 和物体交互。

### Phase 5: 多任务联合训练
- Loss = λ1 * RGB loss + λ2 * depth loss + λ3 * state loss + λ4 * collision loss + λ5 * perceptual/SSIM loss。
- 静态物体和 padding 物体需要在 loss 中 mask 掉。
- 最终形成可替换模型、可复现实验和可扩展指标体系。


## 六、替代方案

### 方案A: 从 SlotPi 论文复现 (最前沿)
- arxiv: 2506.10778
- Hamiltonian physics + slot attention
- 代码未开源, 需要自己实现
- 工作量: 大 (2-3周)

### 方案B: PhysGen 逆向 (最实用)
- github: stevenlsw/physgen ★349
- 思路: PhysGen 从物理参数→视频, 做逆映射: 视频+GT状态→物理参数预测
- 可以用数据做 supervised training
- 工作量: 中 (1-2周)

### 方案C: GNN-first (最直接利用力矩阵)
- 用 PyG 搭 GNN
- 节点 = 物体 (属性+状态+视觉特征)
- 边 = 力向量
- 预测下一时刻物理状态
- 参考: zhouxian/GNS-PyTorch
- 工作量: 中 (1-2周)

### 方案D: DINOv2 + GNN + Decoder (最模块化)
- DINOv2 提取视觉特征 (冻结, 只调后几层)
- GT mask 做 ROI pooling → per-object visual features
- GNN 处理物体交互 (用力矩阵做边权重)
- Transformer 做时序预测
- 多头解码器输出 RGB + depth + mask + physics state
- 工作量: 中-大 (2-3周)


## 七、代码库速查表

| 用途 | 推荐代码库 |
|------|-----------|
| 数据加载/训练 | PyTorch |
| 视觉编码 | 轻量 CNN / ViT / DINOv2 |
| 物体级特征 | GT mask ROI pooling / slot-based encoder |
| 时序预测 | Transformer / SSM / ConvLSTM / video U-Net |
| GNN 物理交互 | PyTorch Geometric / 自定义 message passing |
| 生成式预测 | diffusion video predictor |
| 物理辅助头 | MLP state decoder / collision classifier |
| Slot-based 候选 | SlotFormer (已有代码，可作为候选，不作为唯一方案) |
| 刚体物理引擎(参考) | PhysGen (Bullet wrapper) |
| 可微物理(参考) | Taichi / Brax / NVIDIA Warp |


## 八、最终推荐

**最佳方案: 先完成 `physics_pred/` baseline，再按指标迭代架构**

理由:
1. 第一优先级是跑通真实数据结构，而不是提前押注某个模型。
2. 数据包含 RGB、depth、mask、属性、动态状态和力矩阵，baseline 必须先把字段读取、padding、loss mask 和评估指标做扎实。
3. 统一输出 6 个文件后，可以低成本替换模型并做 ablation。
4. 三项评估能覆盖主要目标：视频预测质量、物理状态一致性、碰撞事件判断。
5. SlotFormer 仍可作为 object-centric 候选，但不再是技术路线的前置限制。

不推荐:
- 从零搭 SAVi+GNS: 工作量大, JAX/TF/PyTorch混用
- 等 SlotPi 开源: 不确定什么时候出
- 第一版直接上重型 diffusion: 训练成本高, 不利于快速验证数据和评估闭环
